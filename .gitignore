# OS
.DS_Store
Thumbs.db

# Large / rights-blocked Bible data - never commit (CONTENT_RIGHTS.md BLOCKED)
bsb/
*.zip

# Draft assets excluded from initial docs-only commit (un-ignore when approved)
# Remove a line when you intentionally add that asset.
/screens/
/copied images/
PRODUCT CONCEPT.pdf

# Secrets - never commit (SECURITY.md)
.env
.env.*
!.env.example
*.pem
*.key

# Future Expo / Node / build outputs (Phase 1+)
node_modules/
.expo/
dist/
build/
.web/

# Local-only design inputs - never commit (owner direction)
# Source HEICs and converted monitor-photo PNGs stay on the local machine.
# NOTE: do not blanket-ignore *.png - apps/mobile needs its Expo icons.
/heic/
*.heic
*.HEIC
/png_out/

# Python bytecode
__pycache__/
*.pyc
