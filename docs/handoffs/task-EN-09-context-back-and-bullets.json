{
  "attempt_id": "task:EN-09-context-back-and-bullets:attempt-5",
  "task_id": "EN-09",
  "task_title": "Context sheet back navigation and bullet splitting for verse references",
  "result": "PASS — back-stack navigation added, toBullets no longer splits verse references; all tests pass.",
  "user_visible_outcome": "In the passage-context flow, Back (and the card's X) unwinds one card at a time: from a connected person you return to the person you came from, and only the first card exits to reading. The context sheet does not pop back up, and cards appear without animation.",
  "files_changed": [
    "apps/mobile/src/components/sheets/ContextFlow.tsx (context navigation stack; Back unwinds one sheet; close clears it)",
    "apps/mobile/src/content/neh2Draft.ts (toBullets splits only on real sentence ends)",
    "apps/mobile/__tests__/entitychip.test.tsx (backs out of an entity sheet to the context sheet)",
    "apps/mobile/__tests__/draft.test.ts (dotted verse references stay in one bullet)",
    "apps/mobile/src/components/Sheet.tsx (animationType prop)",
    "apps/mobile/src/components/sheets/ContextFlow.tsx (animationType='none' for context-flow sheets)",
    "apps/mobile/src/components/sheets/ContextSheet.tsx, EntitySheet.tsx, TimelineSheet.tsx (forward animationType)"
  ],
  "fixes": {
    "back_navigation": "ContextFlow.pop now exits the flow when the stack has one card and otherwise drops the top card. EntitySheet onClose/onBack and TimelineSheet onClose all call pop, so both the X and the card Back button unwind one level; the context sheet (entry point) is never restored as a step. Timeline button relabelled 'Back'. Context sheets appear instantly (animationType='none').",
    "bullet_splitting": "toBullets now splits only where a sentence terminator is followed by whitespace and a capital letter/opening quote (Hermes-safe, no lookbehind). Book-abbreviation periods such as 'Neh.1' no longer split; '(Neh.1).' followed by a new sentence still does.",
    "animation": "Sheet gained an animationType prop (default 'slide'). ContextFlow passes 'none' to the ContextSheet, EntitySheet and TimelineSheet it manages, so swaps between them are instantaneous; other sheets (e.g. the reader peek card) keep the slide animation."
  },
  "commands_executed": [
    {
      "command": "apps/mobile: npx tsc --noEmit && npx prettier --check . && npx jest (NODE_OPTIONS=--experimental-sqlite)",
      "exit_code": 0,
      "output": "34 suites, 296 tests passed; 0 eslint errors (17 warnings, below baseline)"
    }
  ],
  "tests": {
    "back_navigation": "Opening a person from the context sheet then a connected person: Back returns to the first person; Back again exits to reading with neither the entity nor the context sheet visible.",
    "bullet_splitting": "toBullets('Ruined (Neh.1). Earlier work was stopped (Ezra 4:17-23).') returns exactly two bullets, keeping each reference intact."
  },
  "known_limitations": [
    "Manual iOS/Android verification was not run in this environment; behaviour is covered by component tests.",
    "Events and the timeline are pushed onto the same stack; there is no separate history indicator, only Back."
  ],
  "stop_condition": "Stopped after one-by-one Back navigation and correct bullet splitting were implemented and tested. No publication, no Supabase API change, no other chapter."
}
